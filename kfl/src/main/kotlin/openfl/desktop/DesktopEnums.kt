package openfl.desktop

enum class ClipboardTransferMode {
    CLONE_ONLY,
    CLONE_PREFERRED,
    ORIGINAL_ONLY,
    ORIGINAL_PREFERRED
}

enum class SystemIdleMode {
    KEEP_AWAKE,
    NORMAL
}

enum class NotificationType {
    CRITICAL,
    INFORMATIONAL
}
